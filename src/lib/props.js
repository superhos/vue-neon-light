export default {
    effect: {
        type: String,
        default: 'neon',
    },
    color: {
        type: String,
        default: '#ff00ff'
    },
    fontFile: {
        type: String,
        default: null
    },
    flash: {
        type: Boolean,
        default: false
    }
}